VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# In production this must point to the deployed Node auth/API server.
# Do not leave this as localhost in deployed frontend builds.
VITE_AUTH_SERVER_ORIGIN=https://api.keepfit.it.com

# Optional override. Defaults to VITE_AUTH_SERVER_ORIGIN when unset.
# VITE_API_BASE_URL=https://api.keepfit.it.com
